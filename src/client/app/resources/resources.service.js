(function() {
    'use strict';

    angular
        .module('app.resources')
        .service('resourcesService', resourcesService);

    // Service.$inject = ['dependencies'];

    function resourcesService(dependencies) {

    }
})();